<div class="controls">
  <a id="print" href="/print_from_page/1">Print this</a>
  <a id="preview" href="/preview">Preview this</a>
</div>
<div class="paper">
  <div class="content">
    <%= erb :header %>
    <section class="message">
      <p>Type your message</p>
    </section>
    <script>
      $(function() {
        $(".message p").attr("contenteditable", true);
        var updateLinks = function() {
          var url = window.location.origin + window.location.pathname + "?msg=" + encodeURIComponent($(".message p").text());
          var encodedText = encodeURIComponent(url)
          $("#print").attr("href", "/print_from_page/1?url=" + encodedText);
          $("#preview").attr("href", "/preview?url=" + encodedText);
        }
        $(".message p").text(decodeURI(window.location.toString().split("msg=")[1] || ""));
        updateLinks();
        $(".message p").keyup(function() {
          updateLinks();
        })
      })
    </script>
  </div>
</div>