<div class="info">
  <p>This is a preview of how <a href="<%= @preview.original_url %>">your page</a> will look when printed.</p>
  <p>It's only an approximation, but should be useful for tweaking and adjusting.</p>
</div>

<div class="paper">
  <div class="content">
    <% if @preview.error %>
      <p>There was a problem generating your preview:</p>
      <p class="error"><%= @preview.error %></p>
      <p>Some common issues include:</p>
      <ul>
        <li>not including 'http://' at the start of the URL</li>
      </ul>
    <% else %>
      <img src="<%= @preview.image_path %>">
    <% end %>
  </div>
</div>