<div class="paper">
  <div class="content archive">
    <% @prints.each do |print| %>
      <section class="print">
        <div class="metadata">
          <p>Printed: <%= print.created_at %></p>
        </div>
        <img src="/archive/<%= @printer.id %>/image/<%= print.id %>.png" />
      </section>
    <% end %>
  </div>
</div>